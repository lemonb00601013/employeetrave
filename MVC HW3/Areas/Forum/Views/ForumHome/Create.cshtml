@model MVC_HW3.ViewModel.ForumModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "ForumHome", FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
{
    @*<div class="col-lg-4">
        <nav>
            <ul class="nav nav-justified">
                @foreach (var clas in Model.Fclass)
                {
                    <li>@Html.ActionLink(clas.fFC_Class, "Create", "Home", new { id = clas.fFC_ID }, new { @class = "list-group-item" })</li>
                }
            </ul>
        </nav>
    </div>*@

    @*<div class="form-group">
        @Html.LabelFor(p => p.Fclass.First().fFC_Class, new { @class = "col-sm-2 control-label" })
        <div class="col-sm-10">
            @{
                var classes = new SelectList(ViewBag.data, "fFC_ID", "fFC_Class");
            }
            @Html.DropDownListFor(p => p.Ftitle.FirstOrDefault().fFC_ID, classes, "請選擇", new { @class = "form-control" })
        </div>
    </div>*@

    <div class="form-group">
        @Html.LabelFor(p => p.Fclass.First().fFC_Class, new { @class = "col-sm-2 control-label" })
        <div class="col-sm-10">
            @{
                var cla= new SelectList(ViewBag.datas, "fFC_ID", "fFC_Class");
            }
            @Html.DropDownListFor(p => p.Ftitle.FirstOrDefault().fFC_ID, cla, "請選擇副類別", new { @class = "form-control" })
        </div>
     </div>

    <div class="form-group">
            @Html.LabelFor(p => p.Ftitle.First().fFT_Title, new { @class = "col-sm-2 control-label" })
            <div class="col-sm-10">
                @Html.EditorFor(p => p.Ftitle.FirstOrDefault().fFT_Title, new { htmlAttributes = new { @class = "form-control", placeholder = "Forum Title" } })                
            </div>
        </div>

    <div class="form-group">
            @Html.LabelFor(p => p.Fmessage.First().fFM_Content, new { @class = "col-sm-2 control-label" })
            <div class="col-sm-8">
                @*@Html.EditorFor(p => p.Fmessage.FirstOrDefault().fFM_Content, new { htmlAttributes = new { @class = "form-control", placeholder = "Forum MessageContent" } })*@
                @Html.TextAreaFor(p => p.Fmessage.FirstOrDefault().fFM_Content, new { @id = "input", @name = "input" ,@class = "form-control", placeholder = "Forum MessageContent" })
               
            </div>
        </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-default">發布文章</button>
                @Html.ValidationSummary(false, "", new { @class = "text-danger" })
            </div>
    </div>
        
}

@section scripts{
    <script>

        $(document).ready(function () {
            $('#input').cleditor();
        })
    </script>    
}